import { a as cn, i as answersMatch, n as Button, t as AppShell } from "./app-shell-B1QzbLVb.js";
import { l as useSet, n as Route, u as useStudyStore } from "./router-BFTGDrgj.js";
import { t as Input } from "./input-Bh1lFcdP.js";
import { t as EmptyState } from "./empty-state-DtnF40uE.js";
import { t as buildTest } from "./quiz-5wHUEHYV.js";
import { t as StudyChrome } from "./study-chrome-bG7j2NX_.js";
import { useEffect, useMemo, useState } from "react";
import { Link } from "@tanstack/react-router";
import { jsx, jsxs } from "react/jsx-runtime";
//#region src/routes/sets.$setId.test.tsx?tsr-split=component
function TestPage() {
	const { setId } = Route.useParams();
	const studySet = useSet(setId);
	const bumpMastery = useStudyStore((s) => s.bumpMastery);
	const markStudied = useStudyStore((s) => s.markStudied);
	const [round, setRound] = useState(0);
	const questions = useMemo(() => studySet ? buildTest(studySet.cards, Math.min(12, studySet.cards.length)) : [], [studySet?.id, round]);
	const [index, setIndex] = useState(0);
	const [picked, setPicked] = useState(null);
	const [written, setWritten] = useState("");
	const [tf, setTf] = useState(null);
	const [revealed, setRevealed] = useState(false);
	const [score, setScore] = useState(0);
	const [missed, setMissed] = useState([]);
	const [done, setDone] = useState(false);
	useEffect(() => {
		markStudied(setId);
	}, [setId, markStudied]);
	function restart() {
		setIndex(0);
		setPicked(null);
		setWritten("");
		setTf(null);
		setRevealed(false);
		setScore(0);
		setMissed([]);
		setDone(false);
		setRound((n) => n + 1);
	}
	const q = questions[index];
	function finish(ok) {
		if (!q) return;
		setRevealed(true);
		if (ok) setScore((n) => n + 1);
		else {
			const card = studySet?.cards.find((c) => c.id === q.cardId);
			if (card) setMissed((m) => [...m, card.term]);
		}
		bumpMastery(setId, q.cardId, ok ? 1 : -1);
	}
	function next() {
		setPicked(null);
		setWritten("");
		setTf(null);
		setRevealed(false);
		if (index + 1 >= questions.length) {
			setDone(true);
			return;
		}
		setIndex((i) => i + 1);
	}
	if (!studySet) return /* @__PURE__ */ jsx(AppShell, { children: /* @__PURE__ */ jsx(EmptyState, {
		title: "Set bulunamadı",
		description: "Test açılmıyor.",
		action: /* @__PURE__ */ jsx(Button, {
			asChild: true,
			children: /* @__PURE__ */ jsx(Link, {
				to: "/",
				children: "Kütüphaneye dön"
			})
		})
	}) });
	if (questions.length === 0) return /* @__PURE__ */ jsx(StudyChrome, {
		setId,
		title: studySet.title,
		mode: "Test",
		index: 0,
		total: 0,
		children: /* @__PURE__ */ jsx(EmptyState, {
			title: "Kart yok",
			description: "Test için kart ekle."
		})
	});
	if (done) {
		const pct = Math.round(score / questions.length * 100);
		return /* @__PURE__ */ jsx(StudyChrome, {
			setId,
			title: studySet.title,
			mode: "Test",
			index: questions.length,
			total: questions.length,
			children: /* @__PURE__ */ jsxs("div", {
				className: "mx-auto max-w-md rounded-xl bg-surface p-8 text-center shadow-[var(--shadow-border)]",
				children: [
					/* @__PURE__ */ jsx("p", {
						className: "text-sm text-muted",
						children: "Test sonucu"
					}),
					/* @__PURE__ */ jsxs("p", {
						className: "mt-2 font-display text-5xl font-medium tracking-tight tabular-nums",
						children: [pct, "%"]
					}),
					/* @__PURE__ */ jsxs("p", {
						className: "mt-2 text-sm text-muted",
						children: [
							score,
							" / ",
							questions.length,
							" doğru"
						]
					}),
					missed.length > 0 ? /* @__PURE__ */ jsxs("div", {
						className: "mt-6 text-left",
						children: [/* @__PURE__ */ jsx("p", {
							className: "text-xs font-medium tracking-wide text-muted uppercase",
							children: "Kaçırılanlar"
						}), /* @__PURE__ */ jsx("ul", {
							className: "mt-2 space-y-1 text-sm",
							children: missed.map((term) => /* @__PURE__ */ jsx("li", { children: term }, term))
						})]
					}) : /* @__PURE__ */ jsx("p", {
						className: "mt-4 text-sm text-success",
						children: "Hepsi yerinde."
					}),
					/* @__PURE__ */ jsxs("div", {
						className: "mt-6 flex flex-col gap-2",
						children: [/* @__PURE__ */ jsx(Button, {
							onClick: restart,
							children: "Yeni test"
						}), /* @__PURE__ */ jsx(Button, {
							asChild: true,
							variant: "outline",
							children: /* @__PURE__ */ jsx(Link, {
								to: "/sets/$setId",
								params: { setId },
								children: "Sete dön"
							})
						})]
					})
				]
			})
		});
	}
	if (!q) return null;
	return /* @__PURE__ */ jsxs(StudyChrome, {
		setId,
		title: studySet.title,
		mode: "Test",
		index,
		total: questions.length,
		children: [
			/* @__PURE__ */ jsx("p", {
				className: "text-xs font-medium tracking-wide text-muted uppercase",
				children: q.type === "mc" ? "Çoktan seçmeli" : q.type === "written" ? "Yazılı" : "Doğru / yanlış"
			}),
			/* @__PURE__ */ jsx("h2", {
				className: "mt-3 font-display text-3xl font-medium tracking-tight text-balance",
				children: q.prompt
			}),
			q.type === "mc" ? /* @__PURE__ */ jsx("div", {
				className: "mt-8 grid gap-2",
				children: q.options.map((option) => {
					const chosen = picked === option;
					const show = revealed && (option === q.answer || chosen);
					return /* @__PURE__ */ jsx("button", {
						type: "button",
						disabled: revealed,
						onClick: () => {
							setPicked(option);
							finish(option === q.answer);
						},
						className: cn("rounded-lg bg-surface px-4 py-3.5 text-left text-sm shadow-[var(--shadow-border)]", show && option === q.answer && "bg-success-soft text-success", show && chosen && option !== q.answer && "bg-danger-soft text-danger"),
						children: option
					}, option);
				})
			}) : null,
			q.type === "written" ? /* @__PURE__ */ jsxs("form", {
				className: "mt-8 space-y-3",
				onSubmit: (e) => {
					e.preventDefault();
					if (!revealed) finish(answersMatch(written, q.answer));
					else next();
				},
				children: [
					/* @__PURE__ */ jsx(Input, {
						value: written,
						onChange: (e) => setWritten(e.target.value),
						placeholder: "Yanıtın",
						disabled: revealed,
						autoFocus: true
					}),
					revealed ? /* @__PURE__ */ jsx("p", {
						className: cn("text-sm", answersMatch(written, q.answer) ? "text-success" : "text-danger"),
						children: answersMatch(written, q.answer) ? "Doğru" : `Doğrusu: ${q.answer}`
					}) : null,
					/* @__PURE__ */ jsx(Button, {
						type: "submit",
						className: "w-full",
						children: revealed ? "Devam" : "Kontrol et"
					})
				]
			}) : null,
			q.type === "tf" ? /* @__PURE__ */ jsxs("div", {
				className: "mt-8 space-y-4",
				children: [/* @__PURE__ */ jsx("p", {
					className: "rounded-lg bg-surface px-4 py-4 text-lg shadow-[var(--shadow-border)]",
					children: q.statement
				}), /* @__PURE__ */ jsx("div", {
					className: "grid grid-cols-2 gap-2",
					children: [true, false].map((value) => {
						const label = value ? "Doğru" : "Yanlış";
						const chosen = tf === value;
						const show = revealed && (value === q.answer || chosen);
						return /* @__PURE__ */ jsx("button", {
							type: "button",
							disabled: revealed,
							onClick: () => {
								setTf(value);
								finish(value === q.answer);
							},
							className: cn("h-12 rounded-lg bg-surface text-sm font-medium shadow-[var(--shadow-border)]", show && value === q.answer && "bg-success-soft text-success", show && chosen && value !== q.answer && "bg-danger-soft text-danger"),
							children: label
						}, String(value));
					})
				})]
			}) : null,
			revealed && q.type !== "written" ? /* @__PURE__ */ jsx(Button, {
				className: "mt-6 w-full",
				onClick: next,
				children: "Devam"
			}) : null
		]
	});
}
//#endregion
export { TestPage as component };
